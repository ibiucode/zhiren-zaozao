# 職人自造 backend — Phase 2 Backend Foundation
fastapi>=0.110,<1.0
uvicorn[standard]>=0.27
SQLAlchemy>=2.0,<3.0
pydantic>=2.5,<3.0
pydantic-settings>=2.1
email-validator>=2.0
python-dotenv>=1.0
# Admin 驗證（Phase 3）：JWT。密碼雜湊用標準庫 hashlib，故不需 bcrypt/passlib。
PyJWT>=2.8
# 檔案上傳（Phase 5）：FastAPI UploadFile 需要。網格分析用標準庫，不需 trimesh/numpy。
python-multipart>=0.0.9
# PostgreSQL 驅動（正式環境 / Render）；本機改用 SQLite 時不會被用到。
psycopg2-binary>=2.9
