# 前端環境變數範例（複製為 .env 後再填值；.env 不應提交）
# Vite 規定前端可讀取的變數需以 VITE_ 開頭。

# 後端 API base URL。Phase 1 留空即可（會走 src/data 的 mock 資料）。
# Phase 2 接上 FastAPI 後填入，例如：https://api.example.com
VITE_API_BASE_URL=

# 是否使用前端 mock 資料（true = 用 src/data；false = 打 VITE_API_BASE_URL）。
# 預設行為：未設定時，若 VITE_API_BASE_URL 為空則自動使用 mock。
VITE_USE_MOCK=true
